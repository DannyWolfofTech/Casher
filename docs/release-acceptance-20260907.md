# Casher release acceptance — 7 September 2026

**The hardened CSV web core is deployed. The full requested launch is not yet production ready.** This report distinguishes shipped behavior from unverified integrations and owner-only boundaries. No purchase, real charge, store submission, Stripe identity/contact change or legal agreement acceptance occurred during this release work.

**Latest verification:** frontend `9685c91` is deployed with the Zoho correspondence disclosure; all 16 live desktop/mobile checks passed. Android was rebuilt and signature-verified. The refreshed iPhone SDK/simulator Release builds and three iOS UI tests passed; [current artifacts and hashes](mobile-readiness.md) supersede historical references below. Gmail's Zoho SMTP authentication and sender confirmation passed, followed by a real Yahoo Inbox delivery with SPF/DKIM/DMARC passing and no personal Gmail address in received headers. The [provider-export database restore passed](recovery-drill-20260907.md), but full service recovery remains blocked. TrueLayer Data V3 access remains denied; support was contacted.

## Deployed version and checkpoints

Production: https://trycasher.com (alias https://trycasher-com.lovable.app).

- Current frontend source: `9685c91e37646d2b52f6f3a4e5a3ca03a312fb2a`; deployment `994e3640-8522-430f-b881-43cde613064a`. The live custom domain serves `Privacy-osjXG75S.js` with the new Zoho correspondence disclosure. All 16 desktop/mobile production browser checks passed. This supersedes the earlier frontend deployment references in the historical sections below.
- Current billing backend source: `fbeee491d983d86ec7153faab31be2ad271f2afe`. Lovable confirmed deployment of all seven affected functions with no errors: `check-subscription`, `create-checkout`, `create-checkout-session`, `customer-portal`, `stripe-webhook`, `reconcile-billing`, `delete-account`. Provider request `umsg_01m1y35hytfmvbfvm33p4662st`; confirmation `main:agent#00000000134878#don:2NHJNFJK`. The other backend functions remain on the reviewed `cd40d1d` implementation. No migrations, secrets, frontend assets or purchase gates changed in this deployment.
- Major hardening checkpoint: `6a9cca51091362fc8353beb3d3014792c1056e7f`; actual-provider unsubscribe/retention fixes: `cd40d1d`; navigation/live acceptance checkpoint: `983d59f`.
- [Hosted Linux CI](https://github.com/DannyWolfofTech/Casher/actions/runs/34121370128): both `check` and `supabase-local` succeeded at `6b292f0`, including 237 unit tests, 33 browser tests, 18 real integration groups and all frozen function checks.
- Current [main CI 34131168489](https://github.com/DannyWolfofTech/Casher/actions/runs/34131168489) passed at `fbeee49`, including both check and fresh Supabase jobs. The identical [hardening-branch run](https://github.com/DannyWolfofTech/Casher/actions/runs/34131168283) also passed on attempt 2; its first attempt failed only because GitHub returned HTTP 504 downloading Deno, after all 33 browser tests had passed. Only that failed job was retried.
- Eight new production migrations from `20260907100000` through `20260907130000` were applied and recorded. Four maintenance jobs are active. See the [runbook](operations-runbook.md).

## Acceptance by subsystem

| Subsystem | Status | Completed and verified | Exact blocker / type | Exact owner action |
|---|---|---|---|---|
| CSV web app and hosting | PRODUCTION READY | Deployed imports, duplicate/replay handling, direction/category correction, history, goals, responsive public/account UI; real production import/export and ownership checks. Local-only/mock bank and fake admin paths removed. | None for the documented GBP, single-bank-account CSV scope. Overall public launch still depends on legal/support rows. | None for deployment. Supply original statements when repairing ambiguous older data. |
| Authentication, authorization and account data | PRODUCTION READY | Server-verified identity/recent sign-in, protected entitlements, RLS/service-only operations, persistent limits, strict origins, bounded requests, deletion sequencing and full saved-data export. Real local Auth recovery and production isolation verified. | Native recovery acceptance is tracked separately; paid-account deletion still needs fresh Stripe acceptance. | None for the verified Free web-account scope. |
| privacy@ inbound mail | PRODUCTION READY | Free encrypted forwarding DNS; external message and actual production maintenance alert both arrived; later native recovery mail reached the privacy alias's Inbox. | Earlier mail landed in Spam; inbox placement is not guaranteed. | Check Inbox and Spam until sender reputation/rules are established. |
| privacy@ sending and replies | NOT PRODUCTION READY | Free Zoho EU mailbox, ownership/SPF/DKIM, dedicated SMTP credential and Gmail sender confirmation complete. Actual Gmail-through-Zoho test reached Yahoo Inbox; recipient SPF/DKIM/DMARC passed and no personal Gmail address was exposed. A reply to privacy reached the established forwarding Gmail Inbox. | The receiving Gmail account differs from the configured sending account. Its direct safe-reply alias is prepared but Zoho requires fresh OneAuth verification for its dedicated SMTP credential; final reply sender/header verification remains. First direct Zoho test reached Spam; later Gmail test reached Inbox after recipient marked the earlier test Not spam. Type: credential / technical acceptance. | Approve the matching OneAuth prompt opened for the receiving-inbox setup. The agent then completes its SMTP alias and verifies an actual reply hides the personal Gmail address. No signup or payment is needed. |
| Stripe billing | NOT PRODUCTION READY | Official CLI authorized without exporting an API key. Full real Stripe sandbox suite passed **30/30**: hosted checkout, original signed activation, renewal/failure/recovery, replay/concurrency, missed-event reconciliation, hosted portal cancellation/expiry and paid-account deletion. Deleted-customer entitlement refresh bug fixed. | Sandbox acceptance uses real Stripe with local Auth/Postgres/edge code; production sales remain paused, business disclosures are incomplete and final live configuration acceptance is separate. Type: legal / production release verification. | Supply approved business disclosures. No further sandbox CLI authorization or real charge is needed. Do not change Stripe identity/contact settings as part of this work. |
| Open Banking | NOT PRODUCTION READY | TrueLayer Data V3 selected; signup, MFA, Casher sandbox creation and credential verification complete. Required V3 scope/endpoint actually tested; owner-approved support request sent and escalated. V1 mock remains retired. | Provider rejects V3 data scope with invalid_scope and connection endpoint with 403. Actual frontend/consent/sync/revocation acceptance remains incomplete. Type: provider entitlement + technical; production regulatory/KYB/commercial gates. | No repeated signup is needed. Await the support reply to the existing Console email; personally handle any agreements/paid production onboarding. The agent resumes V3 verification after access is enabled. |
| Android installable release | NOT PRODUCTION READY | Signed APK/AAB with bundled production assets, API 36, secure native storage, production session/restart/logout and OS export/share acceptance; signing material has a verified protected same-machine backup. | Broader physical-device/recovery/accessibility acceptance, portable key recovery and store metadata/review remain. Type: technical / hardware / credential / store review. | Run the physical checklist on an Android device; create an off-device encrypted signing backup using an owner recovery password. Decide/authorize a Play testing account and any fees separately. No submission occurs without approval. |
| iOS | NOT PRODUCTION READY | Simulator and actual iPhone SDK Release/arm64 targets compiled on hosted macOS; three real UI tests passed. `release-artifacts/ios/Casher-Device-Unsigned.zip` was downloaded and independently validated; no distribution credentials used. | Apple Team ID, signed device artifact, valid Universal Links and signed-device lifecycle/file/auth acceptance absent. Type: credential / hardware / store review; possible paid developer account. | Provide Apple Team ID, membership status and an iPhone/signing route. A purchased MacBook is not required: hosted Mac compilation works, but signing credentials/profile and registered device or approved distribution route are still necessary. Personally approve enrollment/signing agreements/fees when needed. Complete signed-device acceptance before submission. |
| Production hardening | PRODUCTION READY | Auth/RLS/ownership, secret placement, strict CORS, webhook verification, import/billing limits, bounded bodies/timeouts, error redaction and operational retention deployed and tested; dependency audit clean. | This is a scoped engineering verification, not a security certification. Historic data quality and recovery/monitoring rows still apply. | None for the shipped controls. |
| Monitoring and recovery | NOT PRODUCTION READY | Actual fresh logical export restored twice; 29 Auth users, 490 transactions, 34 migrations, all 23 application RLS tables, existing password hashes and effective permissions matched. Ownership/correction/deletion behavior passed and rolled back. All isolated copies removed. Sentry access, explicit owner alert routing/test dispatch and scheduled GitHub run 34144223617 verified. | Provider-managed Vault ciphertext cannot decrypt outside original environment. Storage/configuration, historical-snapshot recovery and full replacement-service cutover remain unexercised; notification inbox receipt unconfirmed. Type: provider secret-recovery / technical / external receipt. | Obtain Lovable's supported reprovisioning/recovery route for email_queue_service_role_key, Cloud environment and object storage; authorize only an isolated existing-plan recovery. Confirm Sentry/GitHub notification receipt. No further database-export approval is needed. |
| Legal/product disclosures | PRODUCTION READY BUT REQUIRES MY FINAL EXTERNAL ACTION | Privacy/Terms/bank-data/deletion/CSV/subscription copy brought into line with shipped behavior; absolute-security/Premium/native availability claims removed. | Legal operator/trading name and public service postal address are missing. Type: legal / owner identity disclosure. | Supply the legal trading/operator name and a public business/service address you authorize publishing; personally approve the resulting Terms/Privacy disclosures. Do not supply a home address or personal phone. |

## Test evidence

- Actual owner-supplied provider export: clean repeatable restore **passed**, 2.36 seconds for database restoration and 3.51 seconds including behavior checks after startup. All baseline comparisons and 96 effective object-permission comparisons passed. Existing password hashes matched; no hash was printed. The database behavior checks and post-test rollback checks passed. See [machine-readable evidence](recovery-drill-20260907.json). This is not a full-service RTO or historical-snapshot restore claim.

- `npm run check`: TypeScript passed; lint zero errors (7 existing Fast Refresh warnings); **237 tests across 29 files passed**; production build passed.
- Browser regression: **33 passed**, including seven About languages and navigation reset. Hosted Linux checks pass after correcting the French/Polish 320px overflow.
- Fresh local Supabase: every migration applied; **18 real Auth/PostgreSQL/edge integration groups passed**, including owner isolation, atomic imports/limits, real recovery mail, password replacement, deletion, service authorization and unsubscribe suppression.
- All **13 frozen Deno checks** passed for backend and email functions in hosted CI at `6b292f0`.
- `npm audit --audit-level=high`: **0 vulnerabilities** in the resolved tree.
- Actual production readonly API/security checks: **9 passed**.
- Actual production public browser checks after publishing `6b292f0`: **16 passed in 43.9s**, 320/390/1440 widths, six routes, navigation and Android association. No backend fixtures in these tests. Screenshots were refreshed and the live in-app Browser reviewed.
- Actual production disposable-account checks: **5 groups passed** for sign-in/RLS, paused checkout/origin/service boundaries, real import/exact amounts/repeated purchases/replay/quota, full export reads and entitlement/ownership-injection denial. Production account deletion also passed and invalidated its session.
- Final Android `4c13598` signed release build, lint and independent v2 signature verification passed. The actual instrumentation XML records **3 passed, 2 skipped**, zero failures: secure storage/recreation, production configuration and invalid cold/warm recovery links passed. Valid-email recovery and the optional disposable-account session test were skipped in this rebuild. The separate full production sign-in/recreation/system CSV selection/replay/export/share/logout test previously passed at `983d59f`; it is not claimed as rerun on the font-only update.
- Hosted macOS Release simulator build and **3 real XCTest UI checks passed** at `4c13598` on iPhone 17 Pro / iOS 26.2. Recovery request, privacy navigation, signed-out navigation/relaunch and rotation passed. The downloaded 1.0.0 ZIP passed independent identity/configuration/architecture/privacy inspection. This does not verify a valid emailed callback, authenticated-session persistence or signed iOS device acceptance.
- Independent production health checks: **4 groups passed**, both locally and in [GitHub Actions](https://github.com/DannyWolfofTech/Casher/actions/runs/34116904108). A 15-minute schedule is configured. As of the final 12:27 UTC check, GitHub had exposed only the initial push run; scheduled execution and failure-notification delivery remain unverified.
- Live Lighthouse 13.4.1 after publication: **mobile 91 performance / 100 accessibility / 100 best practices / 100 SEO** at 12:26:17 UTC; **desktop 99 / 100 / 100 / 100** at 12:26:54 UTC. Mobile FCP was 2.1s, LCP 2.9s, TBT 20ms and CLS 0.006; desktop LCP was 0.7s. The earlier live mobile run was 73/93/100/100 with 4.4s LCP. These are individual controlled lab runs, not field Core Web Vitals or a latency SLA. Exportable HTML/JSON reports are in `release-artifacts/reports/`.
- Fresh real Stripe lifecycle: **30 passed, 0 failed**, completed at **14:04:54 UTC** through official CLI 1.50.10. [Machine-readable results](billing-acceptance-20260907.json) distinguish real provider activity from local application execution. TrueLayer sandbox acceptance and backup restore drill remain **not run**.

## Android artifacts

`release-artifacts/android/casher-1.0.0-release.apk`
SHA-256: `24E8A7A8061C3E31154740E9D693200357596EA50013791BD5A6F00A04301CAC`

`release-artifacts/android/casher-1.0.0-release.aab`
SHA-256: `E12C8D2EA14A715B3D4325D788224CEEC073A3DBC565EC1CF45C907BDA19F106`

Both use application ID `com.trycasher.app`, version 1.0.0 (build 1), and application source `9685c91e37646d2b52f6f3a4e5a3ca03a312fb2a`. APK size: 4,307,292 bytes; AAB size: 4,194,184 bytes. The rebuilt APK retained the original verified RSA-3072 release certificate and installed successfully over the emulator copy. Release assembly, bundle, lint and unit-test Gradle tasks passed. Physical-device acceptance remains open. Artifacts and signing material are intentionally ignored by Git. [Mobile build/owner instructions](mobile-readiness.md) give exact paths and recovery requirements.

## iOS simulator artifact

`release-artifacts/ios/Casher-Simulator.zip` was produced by [hosted macOS build 34120815933](https://github.com/DannyWolfofTech/Casher/actions/runs/34120815933) at `4c135985d1a1b5d90d1ac3d981bdeb3ba842b230`. Version `1.0.0`, build 1, bundle `com.trycasher.app`, iOS 15 minimum, arm64 and x86_64 simulator architectures. SHA-256: `51F5D53795277CEFF9E462DFF49461A0E2566C2998A10E6E4554EE10C87EC041`. It bundles production API/assets and uses Xcode's local simulator signature, verified by `codesign`; it has no Apple distribution signature or provisioning profile. It is for a compatible Mac iOS Simulator, not an iPhone install or TestFlight.

The first complete XCTest run at `e897a03` executed three tests: privacy passed; launch/recovery failed because the unsigned build lacked the Keychain entitlement (`errSecMissingEntitlement`, -34018). The corrected build at `e4dbda3` passed all three tests. The final `4c13598` build also passed all three with pinned CocoaPods dependencies, bundled fonts and the additional scrolled-landscape check, and supersedes earlier artifacts. Secure storage remains mandatory; no browser-storage fallback or disabled security check was introduced. [Apple's entitlement troubleshooting](https://developer.apple.com/forums/thread/114456) describes the observed error. Six real simulator screenshots are retained in `docs/design-audit/assets/ios-*-20260907.png`.

## Remaining known risks and intentionally disabled behavior

- 481 historical transactions lacked reliable direction in the pre-release inspection. Missing original information and duplicates discarded by old code cannot be reconstructed honestly; warnings and manual correction remain. No automatic guessed backfill.
- CSV supports GBP and one bank account; identical purchases across different accounts cannot be distinguished reliably. Possible-subscription detection can miss or misclassify charges; cancellation happens with the service provider.
- New Pro sales are paused in frontend and backend. Premium purchase/marketing is removed. Real sandbox lifecycle acceptance has passed; live configuration and business-disclosure gates remain. The sandbox portal displays the legacy parent label “SpendLeak sandbox”; Stripe identity/public profile/contact fields were left untouched.
- All production bank connectivity, bank endpoints/migrations, bank token storage, bank sync and bank connection UI are disabled/absent. TrueLayer production onboarding has not started.
- Native Google sign-in and native digital-subscription sales remain disabled. No store publication or signed iOS device build is claimed.
- Backups are daily, approximately 14-day retention; up to one day's database loss is possible. Storage files are not included; no PITR or tested restore RTO is claimed. Independent GitHub checks can detect provider outages, but their scheduled execution has no SLA and notification delivery remains unverified.
- Operational mail has reached Spam. The configured Gmail SMTP alias passed sender-header verification; other Gmail accounts must not be assumed to have that alias. Reply-account consistency remains under verification.
- Main JS bundle exceeds 500kB minified; seven lint warnings and Gradle 9 deprecation warnings remain, with current builds/tests passing.
- Retired welcome-email endpoint returns 410; old unconfigured sender, fake referral/admin metrics and old in-memory/mock billing/bank harnesses are archived outside the production path.
- Existing local marketing video/assets were reviewed and preserved under `docs/marketing`; illustrative figures are disclosed. They were not published.

## Approval-review boundaries

Automatic approval review previously rejected exporting the Stripe API-key page because it could disclose secret credentials. That blocker is resolved through the supported official CLI browser authorization: the key was never read/exported. TrueLayer signup/terms and private production recovery-link handling remain separate owner boundaries. Sentry's initial sign-in later proceeded, but its final GitHub identity/email grant was stopped pending specific permission; private Gmail support access was also rejected. The database export's size blocker was resolved through the existing operational-retention policy. Review then stopped **Start export** because the full database copy into Cloud storage and emailed download link require explicit transfer approval. No rejected action was bypassed; no terms, purchase, identity changes or production restore were performed.

## Earlier production cleanup and native evidence — application 983d59f

The disposable production account was deleted through the deployed `delete-account` endpoint at `2026-09-07T11:06:16Z`; its session was rejected afterward. Independent aggregate SQL confirmed zero remaining test Auth/profile/transaction/upload rows and restored the original **29 profiles, 490 transactions and 60 detected subscriptions**. Four maintenance jobs remained active. No existing customer records were exported or altered.

The native file test used a controlled fixture-only quota reset to expose the Free upload UI and reselected the already-imported four-row CSV through Android's actual document picker. Production correctly returned `REPLAY`; the JSON export still contained exactly four transactions. This complements the earlier genuine first-import/quota API checks; it does not claim a second billable upload or real banking data.

Android's `pm get-app-links com.trycasher.app` reported **trycasher.com: verified** after asynchronous verification, using the delivered release certificate. No domain state was manually approved or overridden. Real emailed recovery/confirmation round trips and broader physical-device acceptance remain outstanding.

Final device test: **1 passed** (full production session/file/export/logout flow), in addition to the **2 passed** native storage/configuration checks. The delivered APK remains installed on the dedicated `CasherRelease` Android 16 emulator. The disposable credential file was removed after account cleanup; no credentials were included in the app, repository or release report.

## Recovery follow-up — 7 September, 12:14 BST

A fresh disposable Casher identity under a unique `privacy+release-…@trycasher.com` alias was provisioned without changing DNS or any existing user's account. The signed Android release requested a real password-reset email from production. The provider recorded it as sent, and the browser showed the matching **Reset your password** message in the forwarded Gmail **Inbox** at 12:14 BST. Earlier messages reaching Spam remain relevant; this single Inbox delivery is not an inbox-placement guarantee.

The attempted complete recovery run did **not pass**: it timed out waiting for a callback that was never dispatched. Automatic approval review rejected reading the email's HTML from the private production queue because it contains a bearer verification credential. No recovery link or token was retrieved by that rejected query. Approval for securely handling only this disposable account's one-time link remains pending; alternatively, complete the email-to-app journey manually on an owner-controlled device. No valid warm/cold recovery or password replacement is claimed.

Independent signed-Android emulator testing with deliberately invalid codes **passed** for both cold and warm Activity callbacks. In each case password replacement stayed disabled, an error appeared, and the user could request a new reset link. This is one additional passing device test, bringing the verified native checks to **4**; it does not substitute for valid-link acceptance.

The test account was deleted through the deployed endpoint and its session was rejected. Production was independently checked again: **29 profiles, 490 transactions, 60 detected subscriptions**, zero remaining recovery test identity and zero queued recovery messages for it. The local disposable credential file was removed. The APK/AAB and deployed application code were unchanged at that recovery checkpoint. The later bundled-font release below supersedes those artifacts.

Reproducible opt-in test: `ProductionRecoveryAcceptanceTest` and `tools/mobile/test-production-recovery.ps1`; scoped cleanup: `tools/release/delete-recovery-test-account.mjs`. The helpers must receive newly provisioned disposable credentials through ignored local files, never application assets or public configuration. The previous identity has been removed and cannot be reused. The full-email test is intentionally not a default passing check. Both hosted CI runs for evidence checkpoint `a92db6a` also completed successfully.

## Bundled-font and accessibility release follow-up

The live mobile Lighthouse audit at 12:04 UTC measured performance 73, accessibility 93, best practices 100 and SEO 100, with 4.4s LCP. It identified low-contrast small text, a missing main landmark, an invalid toast viewport role and remote font requests on the rendering path. Application checkpoint `4c13598` bundles the licensed fonts, removes Google font requests, tightens the font/style CSP and corrects those accessibility issues. The full 33-browser regression passed, including a real visible-toast accessibility check. The initial frozen Deno CI check correctly rejected missing font entries in its workspace lock; checkpoint `6b292f0` adds only those two entries, and all 13 frozen function checks passed locally.

The final Android APK was reinstalled on the dedicated Android 16 emulator, its screen visually reviewed and its production app-link association independently reported `trycasher.com: verified`. This rebuild changes presentation/dependency packaging; the previously verified authentication/backend behavior remains unchanged. Valid emailed recovery and physical-device acceptance remain open.

The final public web source, hosted CI, signed Android artifacts, downloaded iOS artifact and live screenshots are recorded above. Only release evidence/documentation changed after the deployed `6b292f0` application checkpoint. The remaining owner-only boundaries have not been bypassed, and the full requested launch is not marked complete.

## Launch-unblocking follow-up

The frontend remains on `6b292f0`; the later billing backend deployment is recorded above. Sentry ingestion and redaction passed against the real browser SDK, Android signing material has a verified protected local backup, and both simulator and iPhone SDK Release targets passed at `952ebbd` with three passing real UI tests. The unsigned iPhone compilation archive and its limitations are recorded in mobile readiness. Owner input remains necessary for SMTP2GO/TrueLayer terms, portable signing recovery, device acceptance, notification access/delivery, actual provider-backup restoration and public legal details. Stripe sandbox authorization and acceptance are complete. Use [the action checklist and exact setup values](launch-unblocking.md) to continue.

## Real Stripe acceptance and operational follow-up

The 7 September CLI run completed with **30 passing checks** and no failures. It used Stripe's hosted sandbox checkout and hosted cancellation portal, real test-clock renewals/failures/recovery, original Stripe-signed webhook delivery, actual Supabase Auth/Postgres and the repository's edge business logic. Only the SDK HTTP transport was replaced in an ignored copy so the official CLI could authenticate requests without exporting its key. Invalid signatures/mode/account, duplicate and stale events, fenced-lease retry, missed webhook reconciliation, deleted customers, cancellation expiry, paid-account deletion, late events, local recovery and ownership checks passed. Its disposable sandbox clock/customers and local users were cleaned up.

Earlier attempts exposed test-query/order assumptions and a real product defect: `check-subscription` rejected a deleted Stripe customer before revoking stale Pro access. The fix permits only authenticated entitlement refresh to use its server-owned customer binding after deletion; checkout and portal still reject deleted customers. Original concurrent events correctly receive retryable failures; the acceptance replayer now retries those within 60 seconds and still explicitly asserts the held-lease failure. One earlier local runtime restart interrupted a portal request; the final successful run required no manual resume or runtime changes.

At this checkpoint, `npm run check` passed (237 unit tests, 7 existing lint warnings), all 33 browser checks passed, all 13 frozen Deno checks passed and npm audit reported zero vulnerabilities. Generated private audit/credential directories are excluded from lint without weakening their ACLs. The CLI transport/verifier files remain local test tooling and are never deployed.

GitHub's owner-requested [failure drill](https://github.com/DannyWolfofTech/Casher/actions/runs/34127331845) failed only its deliberate final step after healthy production checks. The [normal follow-up](https://github.com/DannyWolfofTech/Casher/actions/runs/34128151586) passed. Email receipt remains unverified because its configured destination is not connected. Lovable support supplied the documented export route. The initial physical-size blocker was subsequently resolved; explicit export-transfer approval now prevents the actual restore drill, as recorded below.

After deploying `fbeee49`, the actual live site passed **16 browser checks in 37.9 seconds**, covering 320/390/1440 widths, public pages, the disabled purchase control and the release Android association. Four production health checks passed. Additional actual production requests rejected an invalid webhook signature (400), untrusted-origin preflight (403), and both anonymous checkout requests (401). These are boundary checks, not an authenticated purchase or a new live charge. The protected JWT gateway correctly prevented an anonymous request from reaching the sales gate.

Read-only Stripe dashboard inspection confirmed production account `acct_1SCrpiJXnVWNQOUC` has active destination `we_1UCoCSJXnVWNQOUCjCigFYFj`, named **Casher production billing**, at the correct Supabase `/functions/v1/stripe-webhook` URL. API version is `2025-08-27.basil`; events are `checkout.session.completed`, `customer.subscription.created`, `customer.subscription.updated`, `customer.subscription.deleted`, `invoice.paid`, `invoice.payment_failed` and `invoice.payment_succeeded`. No signing secret was revealed or account setting changed. Matching-secret delivery and actual live lifecycle operation are not inferred from this configuration inspection; new sales remain paused.

## Database capacity and recovery follow-up

The production database was reduced from **6.65 GB to 15.4 MB** by enforcing the already deployed seven-day retention policy on completed cron logs, followed by compaction of that history table only. **1,611,280 expired entries** were removed; the final check retained 446 recent entries and found zero expired entries. All **29 profiles, 490 transactions and 60 detected subscriptions** remained. Production health checks passed throughout. No customer table, paid instance setting, checkout gate or bank gate was changed. [Machine-readable maintenance evidence](recovery-maintenance-20260907.json).

The 15:40 UTC email and billing job starts timed out during compaction. Their next scheduled executions succeeded at 15:41 and 15:45 respectively, and subsequent runs through 15:55 succeeded. Both email queues and the billing backlog were empty. These transient failures are retained and documented; history-table FULL compaction requires a quiet maintenance period in future.

Checkpoint `dfd2346a06175282844801668edd44efa56ad69d` was committed and pushed. Both [main CI](https://github.com/DannyWolfofTech/Casher/actions/runs/34138115572) and [hardening-branch CI](https://github.com/DannyWolfofTech/Casher/actions/runs/34138115532) passed. The hosted preview was opened successfully despite Lovable's stale build warning; no frontend rebuild was needed. The signed Android artifact already contains all current frontend changes, so its recorded path/hash remain authoritative.

The subsequent baseline/evidence checkpoint `3a7891c7ab004e77dd90a7cf346e161b2855f893` also passed both [main CI](https://github.com/DannyWolfofTech/Casher/actions/runs/34140309559) and [hardening-branch CI](https://github.com/DannyWolfofTech/Casher/actions/runs/34140309660). The empty temporary restore container was removed after its isolation checks; the reviewed startup script recreates it when export approval arrives. No production data was copied into that container.

The disposable PostgreSQL target was started with no network, no published ports, no preloaded workers and memory-only storage. A read-only production baseline verified 23 application tables' RLS metadata, schema/policy/function fingerprints, 34 migration versions, Auth/application counts and absence of deleted users. A final production comparison at **15:49 UTC** matched every recorded count and fingerprint. No production export has been loaded. These checks prepare the drill without claiming it passed. GitHub still reported zero scheduled health runs at that final check.

Automatic approval review rejected the enabled **Start export** action because it copies the full database into Casher's Lovable Cloud storage and emails a temporary download link without explicit approval for that transfer. The size blocker is resolved; this is now an access boundary. The retained-backup UI was checked again and exposes only in-place restore buttons, which were not used. An approval prompt is pending in the conversation; after approval the agent can operate the provider UI and continue the isolated restore verification. Sentry's final GitHub identity/email grant and private Gmail support access were separately rejected and remain pending; no rejected action was bypassed.
