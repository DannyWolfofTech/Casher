# Retired experiment — not deployable

These text files preserve the earlier TrueLayer V1 prototype and its synthetic tests for reference. They are excluded from the application, edge-function discovery, test suite and database migration discovery. They contain no provider credentials and have never passed provider-backed acceptance. Casher selected TrueLayer Data V3; see [the current decision](../../docs/open-banking-provider-selection.md). Do not rename these into a production source tree or treat them as an integration.
