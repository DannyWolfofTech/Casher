// Keep the previously deployed URL on the same authenticated, price-checked path.
// Removing only its source/config would leave the old cloud endpoint callable.
import '../create-checkout-session/index.ts';
